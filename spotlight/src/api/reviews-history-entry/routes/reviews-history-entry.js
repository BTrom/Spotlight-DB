'use strict';

/**
 * reviews-history-entry router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::reviews-history-entry.reviews-history-entry');
